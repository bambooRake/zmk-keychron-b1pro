# LalaPadGen2 → Keychron B1 Pro JIS keymap 移植方針

移植元: `C:\0_git\zmk-config-LalaPadGen2\config\lalapadgen2.keymap`
移植先: `C:\0_git\zmk-keychron-b1pro\app\boards\shields\keychron\b1\jis\keychron_b1_jis.keymap`

- - -

## 0\. 物理レイアウト前提

| 項目 | LalaPadGen2 | B1 Pro JIS |
| --- | ----------- | ---------- |
| キー数 | 60%スプリット + 親指/ナビ/マウス列 + トラックパッド | 86キー（ほぼ100%） |
| OSモード | 単一 | **OS切替廃止** → LalaPadの 0/1/2/3 をそのまま割当 |
| 専用キー | なし（FN/数字をレイヤーで実現） | ESC, F1-F12, BSPC, TAB, CAPS, INT3/INT1/NUHS, 矢印 すべて専用キー |

⇒ 60%特有のレイヤー圧縮設定（mt\_tap で1キーに2機能、親指レイヤーキー、マウスtap-dance等）は基本的にB1 Proでは不要 / 不可能。

- - -

## 1\. 移植元 LalaPadGen2 の構成

### 1-1. レイヤー

* `DEFAULT_LAYER`(0): QWERTY + HRM
* `SECONDARY_LAYER`(1): 数字+記号+F1-F12+数字行HRM
* `TERTIARY_LAYER`(2): ナビ+マウス
* `SYSTEM_LAYER`(3): BT/メディア/明度/ボリューム

### 1-2. カスタムbehavior一覧

| 名前 | 種類 | 用途 | 移植要否 |
| --- | --- | --- | ---- |
| `sticky_mod` | macro-one-param | hold時の遅延離脱付きMod | **要** (HRMの基盤) |
| `&mt` 上書き | hold-tap | hold=sticky\_mod, tap=kp | **要** |
| `&lt` 上書き | hold-tap | quick-tap-ms=200 | 不要（B1 Pro keymapで `&lt` 未使用） |
| `mt_tap` | hold-tap | hold=tap\_key (1tapを発火) | 不要（B1 Proに専用キー有） |
| `tap_key` | macro-one-param | mt\_tap の依存 | 不要 |
| `lm` | macro-two-param | layer + mod 同時発火 | 不要（該当comboを移植しない） |
| `mt2` | hold-tap | tap-preferred変種 | 不要（移植元でも未使用） |
| `tap_dance_layer_1and2` | tap-dance | mo1/mo2切替 | 不要（移植元でも未使用） |
| `tp_msc_*` / `tp_mkp_*` | tap-dance | スクロール/マウスボタン | 不要（B1 Proにマウス機能なし） |
| `zip_dyn_scale*` | カスタム | トラックパッドDPI | 不要（B1 Proに該当機能なし） |

### 1-3. HRM (Home Row Mod) 一覧

| 位置 | キー | hold | 種別 |
| --- | --- | ---- | --- |
| 0 | Q | ESCAPE | mt\_tap **(移植済)** |
| 1 | W | LGUI | mt **(移植済)** |
| 3 | R | INT4 | mt\_tap **(移植済)** |
| 10 | A | LCTRL | mt **(移植済)** |
| 11 | S | LALT | mt **(移植済)** |
| 13 | F | INT5 | mt\_tap **(移植済)** |
| 20 | Z | LSHIFT | mt **(移植済)** |
| 22 | C | CAPSLOCK | mt\_tap **(移植済)** |

### 1-4. Combo一覧

| 名前 | 位置 | 動作 | 移植要否 |
| --- | --- | --- | ---- |
| CONBO\_ESC | 0+1 (Q+W) | ESC | **要** (B1 Pro 30+31 へ) |
| COMBO\_TAB | 10+11 (A+S) | TAB | 不要（B1 ProにTAB専用キー有） |
| COMBO\_TAB\_ALT | 33+4 (mo2親指+T) | lm 2 LEFT\_ALT | 不要（60%親指キー依存） |

- - -

## 2\. B1 Pro JIS で維持する設定（既存）

* F-row全体（ESC, F1-F12 機能, DEL）
* 数字行（GRAVE, 1-0, -, =, INT3, BSPC）
* TAB / CAPS / NUHS / INT1 / INT3 / RET
* 親指列（LCTRL, LWIN/LALT, LCMD/LALT, LANG2, SPACE, LANG1, RCMD/RALT, FN, 矢印）
* ~~既存の `mo 1` / `mo 2` / `mo 3` レイヤー切替構造 (Mac default → Mac Fn → Win default → Win Fn)~~  → **廃止**（OS切替なし、LalaPad の 0/1/2/3 を直接割当）
* BTペアリング系 (`bt_pair_*`, `combo_jz/xl/b/win_l`, `long_press_*`) ※ combo は layer 1 のみ発火
* モード切替行 (mo2, OUT\_BLE, OUT\_24G, OUT\_CHG, OUT\_CHGD)
* `keychron_b1_jis.conf`（必要最小限の変更：`CONFIG_ZMK_POINTING=y` / `CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y` を追加。マウスキー有効化のため）

- - -

## 3\. 変更チェックリスト

各項目: `[ ]` = やらない / `[x]` = やる
追記指示があれば `=> 指示がある場合はここに入力` の行に書いてください。

### 3-1. behavior追加（ヘッダ部）

* [x] `sticky_mod` macro を追加（LalaPadGen2と同一定義）
=> 指示がある場合はここに入力
* [x] `&mt` 上書き: `bindings = <&sticky_mod>, <&kp>; flavor = "balanced"; quick-tap-ms = <0>;`
=> 指示がある場合はここに入力
* [x] `&lt` 上書き (`quick-tap-ms = <200>`)
=> 指示がある場合はここに入力（現状B1 Proで `&lt` 未使用のため不要と判断）
* [x] `mt_tap` / `tap_key` behavior 追加
=> 指示がある場合はここに入力
* [x] `lm` / `mt2` / `tap_dance_layer_1and2` behavior 追加
=> `tap_dance_layer_1and2`は不要
* [x] `tp_msc_*` / `tp_mkp_*` / `zip_dyn_scale*` 追加
=> `zip_dyn_scale*`は不要

### 3-2. HRM 反映（default\_layer = Mac layer 0）

* [x] 位置43 A: `&kp A` → `&mt LCTRL A`
=> 指示がある場合はここに入力
* [x] 位置44 S: `&kp S` → `&mt LALT S`
=> 指示がある場合はここに入力
* [x] 位置57 Z: `&kp Z` → `&mt LSHIFT Z`
=> 指示がある場合はここに入力
* [x] 位置30 Q: `&mt_tap ESCAPE Q`
=> 指示がある場合はここに入力（B1 Proに ESC専用キー有のため不要と判断）
* [x] 位置31 W: `&mt LGUI W`
=> 指示がある場合はここに入力
* [x] 位置33 R: `&mt_tap INT4 R`
=> 指示がある場合はここに入力（B1 ProにLANG1=INT4 専用キー有）
* [x] 位置46 F: `&mt_tap INT5 F`
=> 指示がある場合はここに入力（B1 ProにLANG2=INT5 専用キー有）
* [x] 位置59 C: `&mt_tap CAPSLOCK C`
=> 指示がある場合はここに入力（B1 ProにCAPS専用キー有）

### 3-3. ~~HRM 反映（layer\_two = Win layer 2）~~ 廃止

* [ ] ~~位置43 A: `&kp A` → `&mt LCTRL A`~~
=> Win層 (旧 layer 2) は廃止。layer 2 は LalaPad TERTIARY (ナビ/マウス) として再構築済み。
* [ ] ~~位置44 S: `&kp S` → `&mt LALT S`~~
=> 同上
* [ ] ~~位置57 Z: `&kp Z` → `&mt LSHIFT Z`~~
=> 同上

### 3-4. HRM 反映（Fn層: layer\_one / layer\_three）

* [ ] Fn層にもHRMを入れる
=> 指示がある場合はここに入力（Fn層はF1-F12/BTペアリング主体で性質が異なるため非推奨）

### 3-5. Combo追加

* [ ] **Q+W=ESC** combo（B1 Pro位置30+31）
=> 不要（B1 ProにESC専用キー有）
* [ ] A+S=TAB combo
=> 不要（B1 ProにTAB専用キー有）
* [x] **mo2(LANG2)+T = `&lm 2 LEFT_ALT`** combo (LalaPad COMBO\_TAB\_ALT 移植)
=> B1 Pro 位置 `<72 34>`、layers `<0>`、slow-release。Tertiary層+LEFT\_ALT。LalaPadの「mo 2 ボタン + T」と同じ意味的トリガー。
* [x] 既存B1 Pro combos (`combo_jz/xl/b/win_l`) の `layers` を `<1 3>` → `<1>` に縮小
=> OS切替廃止に伴い layer 3 (旧Win Fn) も別用途化、layer 1 (Fn) のみ発火

### 3-6. 数字/記号レイヤー（LalaPadGen2 SECONDARY）

* [x] B1 Pro Fn層に N1=LCTRL等の数字行HRMを反映
=> 指示がある場合はここに入力（B1 Pro数字行は独立キーで存在、Fn層はF1-F12用途のため不要）
* [x] B1 Pro Fn層に SECONDARY のN3=LBKT/N4=RBKT/N6=LSHIFT等を反映
=> 指示がある場合はここに入力

### 3-7. ナビ/マウスレイヤー（LalaPadGen2 TERTIARY）

* [x] ナビ系（HOME/END/PgUp/PgDn）をB1 Pro Fn層に反映
=> 指示がある場合はここに入力（B1 Pro既存 layer\_one/three の右下に既に存在）
* [x] マウス系 (mkp/msc) を反映
=> 指示がある場合はここに入力（B1 Proにマウス機能なし）

### 3-8. システムレイヤー（LalaPadGen2 SYSTEM → B1 Pro layer 3）

* [x] **左手側のみ** layer 3 に反映 (C\_MUTE / 明度 / メディア / 矢印 / Vol / BSPC / PG\_UP/DOWN / DEL)
=> 左手側だけ移植。右手側 (BT\_SEL / sys\_reset / bootloader / BT\_CLR / mkp MB1 等) は **B1 Pro既存の layer 1 (Fn) + combos (`combo_jz`等)** で代替済みのため `&trans` でフォールスルー。
* [ ] BT\_SEL / sys\_reset / bootloader / out OUT\_TOG を layer 3 右手側にも反映
=> 不要。B1 Pro layer 1 の `bt_pair_*` (Fn+1〜4) と `combo_jz` (Fn+J+Z) / `combo_b` (Fn+B) 等で既に同等機能あり。
* [ ] `zip_dyn_scale ZDS_*` (トラックパッドDPI) を反映
=> B1 Proにトラックパッドなし → `&trans` でスキップ。
* [ ] **layer 3 へのアクセス手段** (`&mo 3` のバインド先)
=> 現状未バインド（layer 3 は定義のみ存在、到達経路なし）。今後、独立キー or combo で割当を別途決める。

### 3-9. confファイル

* [x] `keychron_b1_jis.conf` を変更
=> マウスキー (`&mkp` / `&msc` / `tp_*`) のため `CONFIG_ZMK_POINTING=y` と `CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y` のみ追加。Trackpad/RGB/Sleep 等ハードウェア固有設定は移植せず。

### 3-10. board / shield / matrix

* [ ] `keychron_b1_jis.overlay` の matrix-transform を変更
=> 指示がある場合はここに入力（**禁止項目**：物理配置・基板定義は変更しない）
* [ ] `Kconfig.shield` を変更
=> 指示がある場合はここに入力（**禁止項目**）

- - -

## 4\. 確認事項（要回答）

### Q1. Mac layer の HRM 修飾キーの方向性

* 案A（**現方針**）: LalaPadGen2と同じ `LCTRL / LALT / LSHIFT`
* 案B: Mac慣習に合わせ `LGUI(LCMD) / LALT / LSHIFT`
=> 案A

### Q2. Win layer の HRM 修飾キー

* 案A（**現方針**）: Macと同じ `LCTRL / LALT / LSHIFT`
* 案B: 別構成
=> 案A

### Q3. ~~Q+W=ESC combo の有効レイヤー~~ → 不要に変更

* ~~案A: `layers = <0 2>`~~
* ~~案B: 全レイヤー~~
* 案C（**確定**）: 不要 (B1 ProにESC専用キー有)
=> 案C 確定

### Q4. tapping-term-ms / quick-tap-ms / require-prior-idle-ms

LalaPadGen2は `&mt` の tapping-term は明示せずZMK標準(200ms)依存、`quick-tap-ms = <0>`。

* 案A（**現方針**）: LalaPadGen2踏襲
* 案B: 別の値 (例 `tapping-term-ms = <200>; quick-tap-ms = <175>; require-prior-idle-ms = <125>;`)
=> 案A

### Q5. その他、移植したい項目があれば

=> OS切替廃止。LalaPad の 0/1/2/3 をそのまま B1 Pro の 0/1/2/3 に割当。
（更新ログ:
  - layer 3 = System 層も対応 (BT/メディア/明度/ボリューム/sys\_reset/bootloader)
  - layer 3 へのアクセスは現状未バインド (`&mo 3` を割当てない判断)
  - 既存B1 Pro combos の `layers` は `<1>` のみに縮小
  - 位置72 (LANG2/無変換) を `&mo 2` に変更 → スペース左でTertiary層に直接アクセス。位置81 (側面モード切替ボタン) も `&mo 2` のまま残置
  - 位置73 (SPACE) を `&lt 1 SPACE` に変更 → tap=SPACE, hold=layer 1 (Secondary)。LalaPad と同じ流儀。`&lt { quick-tap-ms = <200>; }` 上書き済で誤発火対策あり
  - combo_tab_alt の key-positions を `<76 34>` → `<72 34>` (LANG2/mo2 + T) に変更してLalaPad意味的再現）

- - -

## 5\. ビルド時の注意点

1. `&mt` / `&lt` 上書きは `/` ノードの外（ルートスコープ）に記述。LalaPadGen2と同じ書式。
2. `sticky_mod` / `tap_key` / `mt_tap` / `mt2` / `lm` / `tp_*` ラベルは B1 Pro 既存keymapで未使用 → 衝突なし。
3. 新combo (`combo_tab_alt`) は既存 `combos1` ノード内に追記。
4. `&mt LCTRL A` / `&mt_tap ESCAPE Q` 等の bindings は ZMK標準。`&kp` 互換のためビルドエラーにならない。
5. matrix transform は変更しないため、キー位置インデックス (0-85) は維持。各レイヤー86キー必須。
6. `keychron_b1_jis.conf` に `CONFIG_ZMK_POINTING=y` / `CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y` を追加（layer 2 の `&mkp` / `&msc` / `tp_*` 必須要件）。
7. layer 3 (System) は定義のみ・到達経路なし。手動到達したい場合は別途 `&mo 3` の置き場所を決める必要あり。

- - -

## 6\. 進行手順

1. 本ファイルのチェックリストを確認・修正
2. 「OK 進めて」で keymap 編集を実施
3. 編集後 diff と最終内容を提示
4. 必要に応じて `west build` 実行（手動 or 別途指示）

- - -

## 7\. 現在の最終構成（実装反映スナップショット）

### 7-1. レイヤー構成

| Layer | 名前 | 内容 | アクセス方法 |
| ----- | ---- | ---- | ----------- |
| 0 | `default_layer` | LalaPad DEFAULT (QWERTY + HRM)。F-row / 数字行 / 親指列はB1 Pro既存。Q/W/R/A/S/F/Z/CにHRM | 起動時 |
| 1 | `layer_one` | LalaPad SECONDARY (数字HRM/記号/F1-F12)。F-row=F1-F12, 数字行=BTペアリング系を維持 | 位置73 (SPACE) ホールド or 位置76 (FN) ホールド |
| 2 | `layer_two` | LalaPad TERTIARY (ナビ/マウス)。`&mkp` `&msc` `tp_*` 含む | 位置72 (LANG2/無変換) ホールド or 位置81 (側面ボタン) ホールド |
| 3 | `layer_three` | LalaPad SYSTEM の **左手側のみ** (C\_MUTE / 明度 / メディア / 矢印 / Vol / BSPC / PG\_UP/DOWN / DEL)。右手側は `&trans` (B1 Pro layer 1 で代替) | **未バインド** (到達経路なし) |

### 7-2. 親指列の構成（layer 0）

| 位置 | 物理キー | バインド | 備考 |
| --- | ------ | ------- | ---- |
| 69 | LCTRL | `&kp LCTRL` | 既存維持 |
| 70 | LWIN/LALT | `&uc LALT` | 既存維持 |
| 71 | LCMD/LALT | `&uc LCMD` | 既存維持 |
| 72 | LANG2 (無変換) | **`&mo 2`** | Tertiary層momentary (NEW) |
| 73 | SPACE | **`&lt 1 SPACE`** | tap=SPACE, hold=Secondary層 (NEW) |
| 74 | LANG1 (変換) | `&kp LANG1` | 既存維持 |
| 75 | RCMD/RALT | `&uc RCMD` | 既存維持 |
| 76 | FN | `&mo 1` | Secondary層momentary (既存) |
| 77-80 | ←↑↓→ | `&kp LEFT/UP/DOWN/RIGHT` | 既存維持 |
| 81 | 側面モード切替 | `&mo 2` | Tertiary層momentary (旧OS切替→Tertiary用に転用) |
| 82-85 | OUT切替 | `&out OUT_BLE/24G/CHG/CHGD` | 既存維持 |

### 7-3. Combo 一覧（実装後）

| 名前 | layers | 位置 | 動作 | 由来 / 備考 |
| --- | ------ | ---- | ---- | ---- |
| `combo_tab_alt` | `<0>` | `<72 34>` (mo2/LANG2 + T) | `&lm 2 LEFT_ALT` (Tertiary+ALT) | LalaPad COMBO\_TAB\_ALT 移植 |
| `combo_jz` | `<1>` | `<49 57>` (J+Z) | `&long_press_recover 0 0` | B1 Pro既存・BT recover |
| `combo_xl` | `<1>` | `<58 51>` (X+L) | `&long_press_change 0 0` | B1 Pro既存・OS切替廃止により実質無意味だが残置 (`&out OUT_FN`) |
| `combo_b` | `<1>` | `<61>` (B) | `&out OUT_BAT` | B1 Pro既存・バッテリー出力 |
| `combo_win_l` | `<1>` | `<70>` (LWIN相当) | `&long_press_fn_win 0 0` | B1 Pro既存・OS切替廃止により実質無意味だが残置 (`&out OUT_FN_WIN`) |
| `combo_t0..t6` | `<4>` | (省略) | (省略) | B1 Pro既存・layer 4 dead code (発火しない) |

### 7-4. confファイル変更

```diff
+ # Required for &mkp / &msc on layer 2 (LalaPadGen2 TERTIARY port).
+ # B1 Pro has no physical pointer; these emit USB HID mouse events only.
+ CONFIG_ZMK_POINTING=y
+ CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y
```

### 7-5. バグ修正履歴

| 日付 | 修正箇所 | 内容 |
| --- | ------ | ---- |
| 反映済 | layer\_two row 1 (number row) | `&trans` 過剰個数 → 15個に修正 (matrix-transform 整合) |
| 反映済 | layer\_three row 1 (number row) | `&trans` 過剰個数 → 15個に修正 (matrix-transform 整合) |
| 反映済 | layer\_two / layer\_three row 5 (thumb row) | `&trans` 11個 → 12個に修正 (matrix-transform 整合) |
| 反映済 | layer\_three row 2 / row 3 右手側 | LalaPad SYSTEM の右手側 (BT\_SEL / sys\_reset / mkp MB1 / BT\_CLR / BT\_CLR\_ALL / bootloader) → 全て `&trans` に変更。B1 Pro layer 1 の既存BT/system機能で代替するため。 |

### 7-6. キー数検証（最終）

各レイヤーの bindings 行数:

```
Row 0 (F-row)    : 14
Row 1 (number)   : 15
Row 2 (qwerty)   : 13
Row 3 (home)     : 14
Row 4 (bottom)   : 13
Row 5 (thumb)    : 12
Row 6 (switch)   :  5
                  ---
合計             : 86  ← matrix-transform と一致
```

全 4 レイヤー (0/1/2/3) で 86 キーすべて整合。

### 7-7. Layer 3 アクティブバインド一覧

LalaPad SYSTEM の左手側のみ移植。右手側 (Y以降, H以降) は全て `&trans` でフォールスルー。

| 物理位置 | キー | バインド | 動作 |
| --- | ---- | ------ | ---- |
| 30 | Q | `&kp C_MUTE` | ミュート |
| 31 | W | `&mt_tap C_BRIGHTNESS_DEC C_BRIGHTNESS_INC` | hold=明度↓ / tap=明度↑ |
| 32 | E | `&kp K_CONTEXT_MENU` | コンテキストメニュー (App key) |
| 33 | R | `&kp UP` | ↑ |
| 34 | T | `&mt END HOME` | hold=END (sticky) / tap=HOME |
| 43 | A | `&mt LCTRL C_VOLUME_UP` | hold=LCTRL (sticky) / tap=Vol+ |
| 44 | S | `&mt LALT ENTER` | hold=LALT (sticky) / tap=ENTER |
| 45 | D | `&kp LEFT` | ← |
| 46 | F | `&kp DOWN` | ↓ |
| 47 | G | `&kp RIGHT` | → |
| 57 | Z | `&mt LSHIFT C_VOLUME_DOWN` | hold=LSHIFT (sticky) / tap=Vol- |
| 58 | X | `&kp BSPC` | バックスペース |
| 59 | C | `&kp PG_UP` | PgUp |
| 60 | V | `&kp PG_DN` | PgDn |
| 61 | B | `&kp DEL` | Delete |
| その他 | (右手 / F-row / 数字行 / thumb / switch) | `&trans` | フォールスルー |

### 7-8. git diff 概要 (移植元コミットからの累計)

```
keychron_b1_jis.conf   :   +6 / -1
keychron_b1_jis.keymap : +247 / -62
方針.md                 : +約350行 (新規セクション群)
```

(`d63f2181` 移植コミット親 〜 現在 working tree)